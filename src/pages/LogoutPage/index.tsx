import { useTranslation } from "react-i18next";
// import {  } from "./styles";

const LogoutPage = () => {
  const { t } = useTranslation();
  return <section>This is Logout Page</section>;
};
export default LogoutPage;
